<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <%= f.label :tag_id %>
  <br>
  <%= f.collection_check_boxes(  
                        :tag_ids, 
                        Tag.all, 
                        :id, 
                        :name, 
                         {}, 
                         ) %> 
                         <br>
 <!--  <%= f.collection_radio_buttons(
                        :authors,
                        User.all,
                        :id,
                        :name,
                        {}
                        ) %> -->
  <%= f.submit %>

<% end %>